DEPENDENCIES: Flask
Flask-SQLAlchemy
Flask-Login
Flask-WTF
PyMySQL

Step 1: make a folder to hold the GUI project
(make a virtual environment)
python -m venv venv (hit enter)

(activate the virtual environment)
venv\Scripts\activate

(notes: run this in the powershell/cmd terminal)
pip install Flask Flask-WTF 
pip install Flask-SQLAlchemy PyMySQL

pip freeze > requirements.txt
pip install -r requirements.txt

pip install Flask-login

(notes: run this in the powershell/cmd terminal)
python -m pip install pymysql (hit enter)
python -m pip install cryptography(hit enter)

(notes: run this in the cmd terminal)
set FLASK_ENV=development
set FLASK_APP=run_app.py

python setup_db_users.py #cd to where the file is in the dir
# database schema matches the Python models, allowing the script to proceed and hash the passwords.

python run_app.py --- final run



(IF THE CONNECTOR DOESN'T WORK)
(terminal) pip install mysql-connector
python -m pip install mysql-connector-python
python -m pip install mysql-connector-python-rf

pip install Flask-Login Werkzeug

